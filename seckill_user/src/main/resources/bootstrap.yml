spring:
  application:
    name: seckill-user
  cloud:
    nacos:
      server-addr: 43.139.44.154:8848 # 注册中心的地址
      config:
        file-extension: yml # 文件后缀名
    sentinel:
      eager: true
      transport:
        dashboard: localhost:8080
        port: 8722